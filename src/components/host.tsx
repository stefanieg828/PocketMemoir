/**
 * Host mascot intentionally removed — locked direction is sticker-book chrome,
 * not a 3D / moth / polaroid host. Kept as a null stub so old imports stay quiet.
 */
export function Host() {
  return null;
}

export default Host;
